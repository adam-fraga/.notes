// Use node inspect file.js to debug in the chrome navigator (url in port bookmark)

const friends = ['Adam', 'Nana', 'Imn', 'Josef', 'Yoda', 'Baboul', 'Lolo']

friends.forEach((friend) => {
  debugger //Breakpoint
  console.log(friend)
})
